function speakDetail(text,funny=false){if(!('speechSynthesis'in window))return;speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang='ca-ES';u.rate=funny ? 0.92 : 0.86;u.pitch=funny ? 1.35 : 1;const ca=speechSynthesis.getVoices().find(v=>v.lang.toLowerCase().startsWith('ca'));if(ca)u.voice=ca;speechSynthesis.speak(u)}
function toggleDetailFavorite(slug,btn){let f=[];try{f=JSON.parse(localStorage.getItem('favorites')||'[]')}catch{}f=f.includes(slug)?f.filter(x=>x!==slug):[...f,slug];localStorage.setItem('favorites',JSON.stringify(f));btn.classList.toggle('favorite',f.includes(slug))}
document.addEventListener('click',e=>{const speak=e.target.closest('[data-speak]');if(speak){speakDetail(speak.dataset.speak,speak.dataset.funny==='1');return}const fav=e.target.closest('[data-favorite]');if(fav){toggleDetailFavorite(fav.dataset.favorite,fav);return}const mail=e.target.closest('[data-contact-email]');if(mail){e.preventDefault();const address=`noelia.projects${String.fromCharCode(64)}gmail.com`;location.href=`mailto:${address}`}});
if('serviceWorker'in navigator)navigator.serviceWorker.register('../../service-worker.js').catch(()=>{});
